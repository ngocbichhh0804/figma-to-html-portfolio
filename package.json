{
  "name": "glo-shop",
  "version": "1.0.0",
  "private": true,
  "description": "Figma → HTML/CSS: trang Shop của Glo., viết tay bằng CSS thuần. Không build, không JavaScript.",
  "scripts": {
    "dev": "browser-sync start --server --port 3000 --no-notify --files \"index.html,css/**/*.css\""
  },
  "devDependencies": {
    "browser-sync": "^3.0.3"
  }
}
